# 🧪 快速测试指南

## 一键测试（推荐）

### Windows 用户

1. **启动开发服务器**（如果还没启动）：
   ```bash
   npm run dev
   ```

2. **在浏览器中打开** http://localhost:5173 并进入舆情看板

3. **运行测试脚本**：
   ```bash
   cd scripts
   test.bat
   ```
   或者手动运行：
   ```bash
   python test_insert.py
   ```

## ✅ 验证清单

### 实时订阅功能
- [ ] 运行测试脚本后，3条新闻自动出现在页面上
- [ ] **无需手动刷新页面**
- [ ] 新闻时间显示为"刚刚"
- [ ] 浏览器控制台（F12）显示 `New news item: ...`

### 分类筛选功能

#### 点击"全部"按钮
- [ ] 显示所有新闻（包括测试数据）

#### 点击"🔥 高风险"按钮  
- [ ] 只显示标题含 ⚠️ 的高风险新闻
- [ ] 其他新闻被隐藏
- [ ] 按钮变为深色背景（选中状态）

#### 点击"🤖 科技"按钮
- [ ] 显示科技类新闻（应该有2条测试新闻）
- [ ] 人文类新闻被隐藏
- [ ] 按钮变为深色背景（选中状态）

#### 点击"🎨 人文"按钮
- [ ] 只显示人文类新闻（1条测试新闻）
- [ ] 其他新闻被隐藏
- [ ] 按钮变为深色背景（选中状态）

### 时间显示功能
- [ ] 刚插入的新闻显示"刚刚"
- [ ] 旧新闻显示相对时间（如"5天前"）

### 定时刷新功能
- [ ] 等待30秒后，如果有新数据会自动刷新

## 🎯 测试数据说明

测试脚本会插入3条新闻：

1. **🆕 DeepMind 发布最新 AI 编程工具**
   - 类型：科技
   - 风险：否
   
2. **⚠️ 某大型平台发现严重安全漏洞**
   - 类型：科技
   - 风险：是（高风险）
   
3. **🎨 传统文化与现代科技的融合展览**
   - 类型：人文
   - 风险：否

## 🐛 常见问题

### Q: 新闻没有自动出现？

**解决方案：**
1. 检查浏览器控制台（F12）是否有错误
2. 确认 Supabase 连接正常
3. 手动刷新页面，查看是否有新数据
4. 检查 `.env` 文件中的 Supabase 配置

### Q: 筛选按钮不工作？

**解决方案：**
1. 检查浏览器控制台是否有 JavaScript 错误
2. 确认点击时按钮样式是否变化
3. 查看 React DevTools 中的 `activeFilter` 状态

### Q: 时间显示不是"刚刚"？

这可能是因为：
- 测试脚本运行失败，数据没有插入
- 看到的是之前的旧数据
- 解决：重新运行 `python test_insert.py`

## 📞 需要帮助？

如果测试遇到问题，请提供：
1. 浏览器控制台的错误信息（F12 → Console）
2. 测试脚本的输出
3. 具体哪个功能不工作

---

Happy Testing! 🎉
